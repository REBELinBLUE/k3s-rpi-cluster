apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: victoria-metrics
  namespace: argocd
spec:
  project: default

  destination:
    namespace: monitoring
    name: in-cluster

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
    ignoreDifferences:
      - kind: "Secret"
        jsonPointers:
          - /data/admin-password
      - group: apps
        kind: Deployment
        jsonPointers:
          - /spec/template/metadata/annotations/checksum~1secret
    automated:
      prune: true
      selfHeal: true

  sources:
    - chart: victoria-metrics-k8s-stack
      repoURL: https://victoriametrics.github.io/helm-charts/
      targetRevision: 0.91.2

      helm:
        releaseName: vm

        valuesObject:
          argocdReleaseOverride: victoria-metrics

          # VictoriaLogs Agent
          vlagent:
            enabled: true
            spec:
              k8sCollector:
                enabled: true

          # VictoriaMetrics
          vmsingle:
            enabled: true
            ingress:
              enabled: true
              ingressClassName: traefik
              annotations:
                kubernetes.io/tls-acme: "true"
                cert-manager.io/cluster-issuer: letsencrypt-staging
                traefik.ingress.kubernetes.io/router.middlewares: ingress-forward-auth@kubernetescrd
              hosts:
                - metrics.cluster.rebelinblue.com
              path: /vmui/
              tls:
                - secretName: metrics-ingress-tls
                  hosts:
                    - metrics.cluster.rebelinblue.com

          # VictoriaTraces
          vtsingle:
            enabled: true
            ingress:
              enabled: true
              ingressClassName: traefik
              annotations:
                kubernetes.io/tls-acme: "true"
                cert-manager.io/cluster-issuer: letsencrypt-staging
                traefik.ingress.kubernetes.io/router.middlewares: ingress-forward-auth@kubernetescrd
              hosts:
                - traces.cluster.rebelinblue.com
              path: /select/vmui/
              tls:
                - secretName: traces-ingress-tls
                  hosts:
                    - traces.cluster.rebelinblue.com
            spec:
              retentionPeriod: 3d
              storage:
                resources:
                  requests:
                    storage: 10Gi

          alertmanager:
            enabled: true
            ingress:
              enabled: true
              ingressClassName: traefik
              annotations:
                kubernetes.io/tls-acme: "true"
                cert-manager.io/cluster-issuer: letsencrypt-staging
                traefik.ingress.kubernetes.io/router.middlewares: ingress-forward-auth@kubernetescrd
              hosts:
                - alertmanager.cluster.rebelinblue.com
              path: /
              tls:
                - secretName: alertmanager-ingress-tls
                  hosts:
                    - alertmanager.cluster.rebelinblue.com

          # VictoriaLogs
          vlsingle:
            enabled: true
            ingress:
              enabled: true
              ingressClassName: traefik
              annotations:
                kubernetes.io/tls-acme: "true"
                cert-manager.io/cluster-issuer: letsencrypt-staging
                traefik.ingress.kubernetes.io/router.middlewares: ingress-forward-auth@kubernetescrd
              hosts:
                - logs.cluster.rebelinblue.com
              path: /select/vmui/
              tls:
                - secretName: logs-ingress-tls
                  hosts:
                    - logs.cluster.rebelinblue.com
            spec:
              retentionPeriod: 30d
              storage:
                resources:
                  requests:
                    storage: 10Gi

          grafana:
            ingress:
              enabled: true
              annotations:
                kubernetes.io/tls-acme: "true"
                cert-manager.io/cluster-issuer: letsencrypt-staging
              labels: {}
              path: /
              pathType: Prefix
            hosts:
              - grafana.cluster.rebelinblue.com
            tls:
             - secretName: grafana-tls
               hosts:
                 - grafana.cluster.rebelinblue.com

            plugins:
              - victoriametrics-metrics-datasource
              - victoriametrics-logs-datasource

          defaultDashboards:
            annotations:
              argocd.argoproj.io/sync-options: ServerSideApply=true
