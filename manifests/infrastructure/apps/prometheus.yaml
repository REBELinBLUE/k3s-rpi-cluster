apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prometheus
  namespace: argocd
spec:
  project: default

  destination:
    namespace: monitoring
    name: in-cluster

  source:
    chart: kube-prometheus-stack
    repoURL: https://prometheus-community.github.io/helm-charts
    targetRevision: 55.0.0

    helm:
      releaseName: prometheus
      valueFiles:
        - ../values/prometheus/components.yaml
        - ../values/prometheus/operator.yaml
        - ../values/prometheus/alertmanager.yaml
        - ../values/prometheus/prometheus.yaml
        - ../values/prometheus/grafana.yaml
        - ../values/prometheus/rules.yaml

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
