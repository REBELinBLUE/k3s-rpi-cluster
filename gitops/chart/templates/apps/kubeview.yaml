{{- if and (eq .Values.group "apps") (eq (include "cluster-apps.enabled" (dict "appName" "kubeview" "root" $)) "true") }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kubeview
  namespace: argocd
spec:
  project: default

  destination:
    namespace: kubeview
    name: in-cluster

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true

  sources:
    # - chart: kubeview
    #   repoURL: https://code.benco.io/kubeview/deploy/helm
    #   targetRevision: 2.0.6

    - path: deploy/helm/kubeview/
      repoURL: https://github.com/rebelinblue/kubeview/
      targetRevision: probe-values #2.2.0

      helm:
        releaseName: kubeview

        valuesObject:

          loadBalancer:
            enabled: false

          nodePort:
            enabled: false

          livenessProbe:
            initialDelaySeconds: 30

          readinessProbe:
            initialDelaySeconds: 30

          tolerations:
            - effect: NoSchedule
              key: node-role.kubernetes.io/control-plane
              operator: Exists

          nodeSelector:
            node-role.kubernetes.io/control-plane: "true"

          ingress:
            enabled: true
            host: kubeview.cluster.rebelinblue.com
            tlsSecretName: kubeview-tls
            className: traefik
            annotations:
              forecastle.stakater.com/expose: "true"
              forecastle.stakater.com/network-restricted: "true"
              forecastle.stakater.com/icon: https://thumb.wikimedia.org/wikipedia/commons/thumb/3/39/Kubernetes_logo_without_workmark.svg/960px-Kubernetes_logo_without_workmark.svg.png?utm_source=commons.wikimedia.org&utm_campaign=index&utm_content=thumbnail
              forecastle.stakater.com/appName: Kubeview
              forecastle.stakater.com/group: Apps
              kubernetes.io/tls-acme: "true"
              cert-manager.io/cluster-issuer: {{ include "cluster-apps.certManagerIssuer" (dict "appName" "kubeview" "root" $) }}
              traefik.ingress.kubernetes.io/router.middlewares: traefik-forward-auth@kubernetescrd
{{- end }}
